version: '3'

services:
  c-dev:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    tty: true
    # KI-Agent unterstützt: Environment variables for X11 Forwarding
    environment:
      - DISPLAY=host.docker.internal:0